# Copy to ".env" for local development (do not commit .env).
# Values can also be set as environment variables or ASP.NET Core user secrets.

# OpenAI (used when embeddings / LLM extraction are wired; Phase 0 does not require this)
OPENAI_API_KEY=
